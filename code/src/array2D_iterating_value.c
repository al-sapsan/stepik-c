#include <stdio.h>
 
int main(void) 
{
    char game_pole1[3][3] = {1, 2, 3, 
                            4, 5, 6, 
                            7, 8, 9};
    // 1-3 в 1-ю строку, 4 - во 2-ю строку
    char game_pole2[3][3] = {{1, 2}, {3, 4}};
    // 1 и 2 - в 1-ю строку, 3 и 4 - во 2-ю строку 
 
    for(int i = 0;i < 3;++i) {
        for(int j = 0; j < 3; ++j)
            printf("%d ", game_pole1[i][j]);
        printf("\n");
    }

    return 0;
}
// Линейное представление двумерного массива
#define ROWS 3
#define COLS 3

char pole_1[ROWS * COLS] = {1, 2, 3, 
                            4, 5, 6, 
                            7, 8, 9};

// Доступ к элементу (1, 2) — строка 1, столбец 2
char x = pole_1[1 * COLS + 2]; // x = 6

/* Предполагается, что:
pole_1 — это одномерный массив (char pole_1[M * N]), 
эмулирующий 2D-массив размера M×N
i — номер строки (от 0 до M-1)
j — номер столбца (от 0 до N-1)
* Как это работает
Формула i * N + j преобразует 2D-координаты (i,j) в линейный индекс:
i * N — смещение до начала нужной строки (каждая строка содержит N элементов)
+ j — смещение до нужного столбца в строке
*/